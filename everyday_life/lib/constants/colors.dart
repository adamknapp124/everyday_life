import 'package:flutter/material.dart';

class AppColors {
  // Primary color — soft, muted blue-gray for branding & AppBar
  static const Color primary = Color(0xFF007EA7);

  // Secondary / accent color — orange for buttons, highlights, FAB
  static const Color secondary = Color(0xFF81559B);

  // Text colors
  static const Color textPrimary = Color(0xFFF1F2EE); // dark gray for main text
  static const Color textSecondary = Color(
    0xFF34463B,
  ); // medium gray for secondary text

  // Backgrounds
  static const Color background = Color(
    0xFFFAF9F9,
  ); // soft off-white for app background
  static const Color cardBackground = Color(
    0xFFE0E0E0,
  ); // subtle gray for cards or panels

  // Optional additional colors
  static const Color error = Color(0xFFB00020);
  static const Color success = Color(0xFF4CAF50);
}
